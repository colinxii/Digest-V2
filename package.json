{
  "name": "crm-digest",
  "version": "0.0.0",
  "description": "Api to format email content for dynamics crm digest. ",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "npm ci && npm test && echo 'Build completed successfully'",
    "build:prod": "npm ci --only=production",
    "prebuild": "echo 'Starting build process...'",
    "postbuild": "echo 'Build process completed'"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ejs": "^3.1.10",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.2.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.3",
    "supertest": "^7.1.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
